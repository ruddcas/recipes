<%= render 'shared/page_title', title: "Showing all recipes" %>

<div align="center"><%= will_paginate @chefs, list_classes: %w(pagination justify-content-end) %></div>

<%= render 'recipe', obj: @recipes %>

<div align="center"><%= will_paginate @chefs, list_classes: %w(pagination justify-content-end) %></div>
